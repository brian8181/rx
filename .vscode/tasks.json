{
     "version": "2.0.0",
    "tasks": [
        {
            "label": "MAKE DEBUGGDB",
            "type": "shell",
            "command": "make -f ${workspaceFolder}/build/Makefile rx",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "echo",
            "type": "shell",
            "command": "echo ${workspaceFolder}"
        }
    ]
}